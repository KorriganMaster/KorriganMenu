Require :
	jquery 1.6.2 or greater
	jquery-ui 1.8.16 or greater

Options :

	items: Menu items with JSON format.
		format: 
			[
				{
					'title': '<string>',
					
					(optionnal)
					'content': [{'title': '<string>', 'content': [...] | 'url': '<string>'}, {...}] 
					    OR
					'url': '<string>'
				},
				{
					...
				}
			]
			
		Sample:
			Cf demo.
			
			
Methods :

	init -> Call by default with $('<your_element_id>').korriganMenu({items: ...});